import { Box } from '@mui/system';
import React from 'react';

interface Props {
  onClickClose: () => void;
}

export default function RestaurantPopupContent({ onClickClose }: Props) {
  return <Box>home popup</Box>;
}
